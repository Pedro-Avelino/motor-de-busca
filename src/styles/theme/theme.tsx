import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ffffff",
    },
    secondary: {
      main: "#026CB6",
    },
    info: {
      main: "#13B5EA",
    },
  },
});

export default theme;
